import { useEffect, useRef, useState } from "react";
import { supabase } from "@/integrations/supabase/client";

export function AutoSeeder() {
  // Disabled to prevent overwriting catalog data with products.json values
  return null;
}
